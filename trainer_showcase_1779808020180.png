import React, { useState } from 'react';
import { Sparkles, ArrowRight, MessageSquare, Phone, Percent, ShieldCheck } from 'lucide-react';
import { HERO_IMAGE, GYM_CONTACT } from '../data';

export default function Hero() {
  const [copiedCode, setCopiedCode] = useState(false);

  const generateStudentCode = () => {
    // Generate a beautiful persistent or mock code for students
    setCopiedCode(true);
    setTimeout(() => setCopiedCode(false), 3000);
  };

  return (
    <section id="welcome" className="relative min-h-screen flex items-center justify-center bg-[#09090b] pt-24 overflow-hidden">
      
      {/* Cinematic Background Image Layer */}
      <div className="absolute inset-0 z-0">
        <img
          src={HERO_IMAGE}
          alt="Cinematic Red and Black Gym Equipment Landscape"
          className="w-full h-full object-cover object-center filter brightness-[0.25] saturate-125 scale-105 transition-transform duration-1000 scale-100"
          referrerPolicy="no-referrer"
        />
        {/* Dark to Red Gradient Overlays */}
        <div className="absolute inset-0 bg-gradient-to-t from-[#09090b] via-[#09090b]/40 to-[#09090b]/90" />
        <div className="absolute inset-0 bg-gradient-to-r from-[#09090b] via-transparent to-[#09090b]/80" />
        <div className="absolute inset-0 bg-radial-gradient from-transparent via-black/30 to-[#09090b]/90" />
      </div>

      {/* Decorative Red Laser Lines */}
      <div className="absolute top-1/4 left-0 right-0 h-[1px] bg-red-600/30 blur-[1px] transform -skew-y-3 pointer-events-none" />
      <div className="absolute top-1/2 left-0 right-0 h-[1.5px] bg-red-600/20 blur-[2px] transform skew-y-6 pointer-events-none" />

      {/* Hero Content Container */}
      <div className="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 lg:py-24 grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
        
        {/* Text Col */}
        <div className="lg:col-span-7 space-y-6 text-center lg:text-left" id="hero-headlines">
          
          {/* Tagline */}
          <div className="inline-flex items-center gap-2 px-3 py-1 bg-red-950/60 border border-red-500/30 rounded-full text-red-400 text-xs font-mono tracking-widest uppercase animate-bounce mx-auto lg:mx-0">
            <Sparkles className="w-3.5 h-3.5 fill-red-400" />
            <span>PATTABIRAM'S PREMIER FITNESS SANCTUARY</span>
          </div>

          {/* Heading */}
          <h1 className="font-display text-4xl sm:text-6xl lg:text-7xl font-black tracking-tight text-white leading-none">
            FORGE <span className="text-transparent bg-clip-text bg-gradient-to-r from-red-500 to-red-700 font-extrabold shadow-sm">RAW POWER</span> <br />
            AT SITE CART
          </h1>

          <p className="text-zinc-300 text-base sm:text-lg max-w-xl mx-auto lg:mx-0 font-medium leading-relaxed leading-normal">
            A relentless atmosphere carved in custom <span className="text-white font-bold group">Red & Black theme</span>. Packed with state-of-the-art conditioning rigs, expert coaches, and steam therapy in Pattabiram.
          </p>

          {/* Core Perks */}
          <div className="grid grid-cols-2 gap-4 max-w-md mx-auto lg:mx-0 text-left pt-2 font-display">
            <div className="flex items-center gap-2 text-zinc-300">
              <ShieldCheck className="w-5 h-5 text-red-500 flex-shrink-0" />
              <span className="text-sm font-semibold">Premium Equipment</span>
            </div>
            <div className="flex items-center gap-2 text-zinc-300">
              <ShieldCheck className="w-5 h-5 text-red-500 flex-shrink-0" />
              <span className="text-sm font-semibold">Steam Therapy Zone</span>
            </div>
            <div className="flex items-center gap-2 text-zinc-300">
              <ShieldCheck className="w-5 h-5 text-red-500 flex-shrink-0" />
              <span className="text-sm font-semibold">Expert Coaches</span>
            </div>
            <div className="flex items-center gap-2 text-zinc-300">
              <ShieldCheck className="w-5 h-5 text-red-500 flex-shrink-0" />
              <span className="text-sm font-semibold">Overall Rating: 4.9 ★</span>
            </div>
          </div>

          {/* Dual CTAs */}
          <div className="flex flex-col sm:flex-row items-center justify-center lg:justify-start gap-4 pt-4" id="hero-ctas">
            <a
              href={GYM_CONTACT.whatsappUrl("Hi! I checked your awesome Site Cart Gym website and I want to activate my member account. Let's begin!")}
              target="_blank"
              rel="noopener noreferrer"
              className="w-full sm:w-auto px-8 py-4 bg-red-600 hover:bg-red-700 text-white font-display text-sm font-bold uppercase tracking-wider rounded-lg transition-all duration-300 transform hover:-translate-y-0.5 shadow-[0_0_20px_rgba(220,38,38,0.5)] flex items-center justify-center gap-2.5 cursor-pointer"
            >
              <MessageSquare className="w-4 h-4 text-white" />
              WhatsApp Enquiry
            </a>
            <a
              href={`tel:${GYM_CONTACT.phone}`}
              className="w-full sm:w-auto px-8 py-4 bg-zinc-900 border border-zinc-800 hover:border-zinc-700 text-white hover:text-red-500 font-display text-sm font-bold uppercase tracking-wider rounded-lg transition-all duration-300 flex items-center justify-center gap-2"
            >
              <Phone className="w-4 h-4 text-zinc-400" />
              Call us: {GYM_CONTACT.phone}
            </a>
          </div>

        </div>

        {/* Dynamic Card showcasing Student Discount (50% Off) */}
        <div className="lg:col-span-5 relative" id="student-hero-card">
          
          <div className="absolute inset-0 bg-red-600/10 rounded-2xl blur-xl filter" />
          
          <div className="relative bg-zinc-950/90 border-2 border-red-600/40 rounded-2xl p-6 sm:p-8 shadow-[0_0_50px_rgba(220,38,38,0.15)] overflow-hidden">
            
            {/* Corner Badge */}
            <div className="absolute top-0 right-0 bg-gradient-to-bl from-red-600 to-red-800 text-white font-mono text-[10px] font-bold px-3 py-1.5 uppercase rounded-bl-xl tracking-widest">
              COLLEGE EXCLUSIVE
            </div>

            <div className="flex items-center gap-3 text-red-500 mb-4">
              <Percent className="w-8 h-8 p-1.5 bg-red-950 border border-red-500/30 rounded-lg animate-pulse" />
              <span className="font-mono text-sm tracking-widest font-black uppercase">Active Student Discount</span>
            </div>

            <h3 className="font-display text-2xl sm:text-3xl font-black text-white leading-tight">
              FLAT 50% OFF FOR STUDENTS
            </h3>
            
            <p className="text-zinc-400 text-sm mt-2 leading-relaxed">
              Don't let college budgets block your gains. Bring an official collegiate ID card and load standard access paths at absolute half price!
            </p>

            {/* Interactive Coupon Code Generator */}
            <div className="mt-6 bg-zinc-900/80 border border-zinc-800 rounded-xl p-4 relative" id="student-coupon-area">
              <div className="flex justify-between items-center text-[11px] font-mono text-zinc-500 mb-2">
                <span>VERIFIED OFFER</span>
                <span>LIMITED TIME ONLY</span>
              </div>
              <div className="flex items-center gap-2">
                <input
                  type="text"
                  readOnly
                  value="SCSTU50OFF"
                  className="bg-black border border-zinc-850 px-3 py-2 text-red-500 font-mono text-sm font-bold rounded-lg w-full tracking-widest text-center focus:outline-none"
                  id="student-code-input"
                />
                <button
                  onClick={generateStudentCode}
                  className="bg-red-600 hover:bg-red-700 text-white font-mono text-xs font-bold px-3 py-2.5 rounded-lg transition-transform duration-200 active:scale-95 whitespace-nowrap flex items-center justify-center"
                  id="generate-coupon-btn"
                >
                  {copiedCode ? 'Claimed!✓' : 'Claim Now'}
                </button>
              </div>

              {copiedCode && (
                <p className="text-[10px] text-green-500 font-mono mt-2 text-center animate-fade-in animate-pulse">
                  ✓ Code "SCSTU50OFF" saved! Redirecting to WhatsApp pre-applied!
                </p>
              )}
            </div>

            <div className="mt-6 pt-4 border-t border-zinc-900 flex justify-between items-center">
              <div>
                <span className="text-[10px] text-zinc-500 uppercase font-mono block">Special Student Monthly Rate</span>
                <span className="text-xl font-black text-white font-mono">₹750 <span className="text-xs text-zinc-500">/mo (Was ₹1500)</span></span>
              </div>
              <a
                href={GYM_CONTACT.whatsappUrl("Hi! I am a student and I want to claim the 50% discount with the code: SCSTU50OFF. Here are my details.")}
                target="_blank"
                rel="noopener noreferrer"
                className="bg-zinc-900 border border-zinc-800 hover:border-red-600 text-zinc-300 hover:text-white px-4 py-2 text-xs font-bold rounded-lg transition-all duration-300 flex items-center gap-1.5"
                id="claim-student-cta"
              >
                <span>Activate Rate</span>
                <ArrowRight className="w-3.5 h-3.5 text-red-500" />
              </a>
            </div>

          </div>

        </div>

      </div>

      {/* Slide-Down Anchor */}
      <div className="absolute bottom-6 left-1/2 -translate-x-1/2 z-10 flex flex-col items-center gap-1 opacity-60">
        <span className="text-[9px] font-mono tracking-widest text-zinc-400 uppercase">Discover more</span>
        <div className="w-1.5 h-6 bg-red-600 rounded-full animate-bounce" />
      </div>

    </section>
  );
}
